{
  "background": { "scripts": ["background.js"] },
  "permissions": ["tabs", "history", "bookmarks", "storage", "activeTab", "scripting", "sessions", "downloads"],
  "browser_specific_settings": {
    "gecko": {
      "id": "commandk@vikramhegde.dev",
      "strict_min_version": "140.0",
      "data_collection_permissions": {
        "required": ["none"]
      }
    }
  }
}
